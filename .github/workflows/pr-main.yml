name: PR to main

on:
  pull_request:
    branches:
      - main

jobs:
  test-main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run smoke + security tests
        run: make test-main
